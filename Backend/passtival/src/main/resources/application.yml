spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/passtival?useSSL=false&allowPublicKeyRetrieval=true
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 100MB

logging:
  level:
    org.hibernate.SQL: debug

file:
  item_img-storage-location: /Users/kmo/Study/toy-project/spring-shop/spring-shop-front/storage/images/item

